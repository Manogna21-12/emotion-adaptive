## Mongo (existing)
# MONGO_URI=

## Postgres / Supabase (required for /progress endpoints)
# Supabase: use the provided Postgres connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres

## Optional tuning
PG_POOL_SIZE=5
PG_MAX_OVERFLOW=10

